# 📖 MT5 カスタムインジケーター実装ガイド
## FX_Trading_Signals_1H5M をMT5に導入する完全手順

---

## 📋 目次
1. [準備・確認](#準備確認)
2. [ステップ1：ファイルの配置](#ステップ1ファイルの配置)
3. [ステップ2：MetaEditorでのコンパイル](#ステップ2metaeditorでのコンパイル)
4. [ステップ3：チャートへの適用](#ステップ3チャートへの適用)
5. [ステップ4：パラメータ設定](#ステップ4パラメータ設定)
6. [ステップ5：動作確認](#ステップ5動作確認)
7. [トラブルシューティング](#トラブルシューティング)

---

## 準備・確認

### ✅ 必須条件

- **MetaTrader 5がインストール済み**
- **FX_Trading_Signals_1H5M.mq5 ファイルを用意**
- **管理者権限でMT5を実行可能な環境**

### ✅ 確認事項

```
MetaTrader 5のバージョン確認方法：
1. MT5を起動
2. 「ヘルプ」メニュー → 「ヘルプについて」
3. バージョン情報を確認（通常 5.xx 系）
```

---

## ステップ1：ファイルの配置

### 📁 MQL5フォルダの場所を特定

**Windows の場合：**

```
C:\Users\[ユーザー名]\AppData\Roaming\MetaQuotes\Terminal\[ターミナルID]\MQL5\Indicators\
```

**例：**
```
C:\Users\yuma\AppData\Roaming\MetaQuotes\Terminal\D0E8209F57EA47CCD5A47F6D12345678\MQL5\Indicators\
```

**macOS の場合：**
```
~/Library/Application Support/MetaTrader 5/MQL5/Indicators/
```

**Linux の場合：**
```
~/.config/MetaTrader 5/MQL5/Indicators/
```

### 🔍 ターミナルIDを探す方法

#### 方法1：MT5メニューから確認
1. MetaTrader 5を開く
2. **ファイル** → **データフォルダを開く**
3. 開いたフォルダの親フォルダ階層を確認
4. フォルダ構造から Terminal\[ID] を確認

#### 方法2：MT5の設定から確認
1. MetaTrader 5を開く
2. **ツール** → **オプション** → **一般**タブ
3. 「データフォルダ」の表示

### 📂 フォルダを開く（推奨方法）

**最も簡単な方法：MT5から直接フォルダを開く**

```
1. MetaTrader 5 を起動
2. 「ファイル」メニュー → 「データフォルダを開く」
   ↓
3. エクスプローラーが開く
4. アドレスバーに以下を入力：
   MQL5\Indicators
   ↓
5. Indicators フォルダが表示される
```

### 📥 ファイルをコピー

1. **FX_Trading_Signals_1H5M.mq5** ファイルを用意
2. **Indicators フォルダ**にコピー＆ペースト
3. ファイルが正しくコピーされたか確認

```
✅ 正しいパス例：
C:\Users\yuma\AppData\Roaming\MetaQuotes\Terminal\D0E8209F...\MQL5\Indicators\FX_Trading_Signals_1H5M.mq5
```

---

## ステップ2：MetaEditorでのコンパイル

### 🔧 MetaEditorを開く

#### 方法1：MT5から開く
```
1. MetaTrader 5 の画面上部
2. 「ツール」メニュー → 「MetaEditor」
   または
3. キーボード：Ctrl + Alt + E
```

#### 方法2：スタートメニューから
```
Windows キー → 「MetaEditor」で検索 → 実行
```

### 📖 ファイルを開く

```
MetaEditor が起動したら：

1. 「ファイル」メニュー → 「開く」
   （または Ctrl + O）

2. ファイルブラウザで以下に移動：
   MQL5\Indicators\
   
3. 「FX_Trading_Signals_1H5M.mq5」 を選択 → 開く
```

### 🔨 コンパイル実行

```
1. 「実行」メニュー → 「コンパイル」
   （または Ctrl + Shift + F9）

2. 画面下部の「コンパイル」ウィンドウに出力が表示される
```

### ✅ コンパイル成功の確認

**成功時の表示：**
```
[OK] Compilation successful
[OK] Waiting for compilation
[OK] Result = 0
```

**失敗時の表示例：**
```
[Error] Compilation error: Unexpected end of file (line xx, column yy)
```

**→ 失敗した場合は「トラブルシューティング」を参照**

### 📂 コンパイル後のファイル確認

コンパイルが成功すると、以下が生成されます：
```
FX_Trading_Signals_1H5M.mq5  ← ソースコード
FX_Trading_Signals_1H5M.ex5  ← コンパイル済みバイナリ（これが実際に使用される）
```

**重要：.ex5 ファイルが Indicators フォルダに自動生成されていることを確認**

---

## ステップ3：チャートへの適用

### 🚀 MT5でチャートを開く

```
1. MetaTrader 5 を起動
2. 左側の「気配値」から対象通貨ペアを選択
   例：USDJPY, EURJPY, EURUSD, XAUUSD
3. **5分足チャート**を開く
   （重要！）
```

### ⏰ 5分足に設定する方法

```
チャート画面の上部にある時間足ボタンから：

[ 1分 ] [ 5分 ] [ 15分 ] [ 1時間 ] ...
          ↑
       ここをクリック

または右クリック → 「チャートの期間」→「5分」
```

### 📊 インジケーターを追加

```
【方法1：メニューから】
1. チャート画面上で右クリック
2. 「インジケーター」→「カスタムインジケーター」
   
【方法2：メニューバーから】
1. 「挿入」メニュー
2. 「インジケーター」→「カスタム」

【方法3：キーボード】
Ctrl + I →「カスタム」タブ
```

### 🎯 インジケーターを選択

```
カスタムインジケーターの一覧が表示されます：

▼ FX_Trading_Signals_1H5M  ← これを選択
  └─ その他のインジケーター

FX_Trading_Signals_1H5M をクリックして選択
```

### 📌 「OK」をクリック

```
クリック後：

1. インジケーター設定ダイアログが表示
2. デフォルト設定のまま「OK」をクリック
   （パラメータ設定は次ステップで行う）

3. チャートにインジケーターが追加される
```

---

## ステップ4：パラメータ設定

### ⚙️ パラメータ設定ダイアログを開く

```
【方法1：チャートから】
1. チャート上で右クリック
2. 「インジケーター」→「FX_Trading_Signals_1H5M」

【方法2：ナビゲーターから】
1. 「表示」メニュー→「ナビゲーター」
2. 左の「インジケーター」タブ
3. 「FX_Trading_Signals_1H5M」をダブルクリック
```

### 🎛️ パラメータ一覧と設定値

| パラメータ | 説明 | 推奨値 | 範囲 |
|----------|------|-------|------|
| **Lookback_Periods** | スイング高安を判定する期間 | 20 | 5～50 |
| **Fib_Level_1** | Fibonacci レベル1 (%) | 38.2 | 30～50 |
| **Fib_Level_2** | Fibonacci レベル2 (%) | 50.0 | 40～60 |
| **Fib_Level_3** | Fibonacci レベル3 (%) | 61.8 | 50～80 |
| **Show_EMA** | EMA ラインを表示 | true | true/false |
| **Show_Fib** | Fibonacci ラインを表示 | true | true/false |
| **Show_Signals** | シグナル矢印を表示 | true | true/false |
| **Show_Info_Panel** | 情報パネルを表示 | true | true/false |
| **Use_Alerts** | アラートを有効にする | true | true/false |
| **Arrow_Size** | 矢印のサイズ | 2 | 1～5 |

### 🎯 推奨設定例

**初心者向け（保守的）：**
```
Lookback_Periods = 20
Fib_Level_1 = 38.2
Fib_Level_2 = 50.0
Fib_Level_3 = 61.8
Show_EMA = true
Show_Signals = true
Use_Alerts = true
Arrow_Size = 2
```

**アグレッシブ（より多くのシグナル）：**
```
Lookback_Periods = 15
Fib_Level_1 = 38.2
Fib_Level_2 = 50.0
Fib_Level_3 = 61.8
Show_EMA = true
Show_Signals = true
Use_Alerts = true
Arrow_Size = 3
```

### ✅ 設定完了

```
1. パラメータを入力
2. 「OK」をクリック
3. チャートに変更が反映される
```

---

## ステップ5：動作確認

### 🎬 チャートを再描画

```
キーボード：Ctrl + R

これにより、現在のチャートデータでインジケーターが再計算されます
```

### 👁️ 確認項目

#### ✅ EMA ラインが表示されているか
```
5分足チャートに以下が見えるはず：
- 青い線（EMA 10）
- 水色の線（EMA 20）
- 緑の線（EMA 40）
- オレンジの線（EMA 80）
- 赤い太線（EMA 200）
```

#### ✅ 情報パネルが表示されているか
```
画面左上または画面内に以下のような表示：
"USDJPY | LONG | Phase: Middle | Entry: OK"
```

#### ✅ シグナルが生成されるか
```
条件が満たされると以下が表示：
- 緑の上向き矢印 ↑（LONG シグナル）
- 赤の下向き矢印 ↓（SHORT シグナル）
```

#### ✅ アラートが鳴るか
```
1. Use_Alerts = true に設定
2. 有効なシグナルが出現
3. ツール → オプション → 通知 で確認
```

### 📊 複数通貨ペアで同時確認

```
推奨通貨ペア：
- USDJPY（ドル円）
- EURJPY（ユーロ円）
- EURUSD（ユーロドル）
- XAUUSD（金）

各ペアの5分足チャートにインジケーターを追加して確認
```

---

## トラブルシューティング

### ❌ 問題：「コンパイルエラー」

**症状：**
```
[Error] Compilation error: Unexpected end of file
```

**原因と解決策：**
```
1. ファイルが破損している可能性
   → FX_Trading_Signals_1H5M.mq5 を再度コピー

2. テキスト形式が間違っている
   → メモ帳で開いて UTF-8 で保存

3. ファイルの末尾に余計な文字がある
   → MetaEditor で開いて確認・削除
```

### ❌ 問題：「インジケーターが一覧に表示されない」

**症状：**
```
カスタムインジケーター一覧に FX_Trading_Signals_1H5M が見当たらない
```

**原因と解決策：**
```
1. ファイルが正しいフォルダにない
   → Indicators フォルダを確認
   
2. コンパイルを実行していない
   → MetaEditor でコンパイルを実行
   
3. MT5を再起動していない
   → MT5 を完全に終了 → 再起動
   
4. .mq5 ファイルの名前が間違っている
   → 「FX_Trading_Signals_1H5M.mq5」か確認
```

### ❌ 問題：「インジケーターを追加できない」

**症状：**
```
インジケーターの一覧は見えるが、選択できない、または追加されない
```

**原因と解決策：**
```
1. チャートが5分足でない
   → チャートの時間足を 5分（M5）に変更
   
2. インジケーターが既に追加されている
   → 既存のものを削除してから再度追加
   
3. MT5のキャッシュが古い
   → 「ツール」→「メタクォーツ言語学習版」→「キャッシュをクリア」
```

### ❌ 問題：「シグナルが表示されない」

**症状：**
```
インジケーターは表示されているが、矢印が出現しない
```

**原因と解決策：**
```
1. Show_Signals = false に設定されている
   → パラメータを確認して true に変更
   
2. 条件が満たされていない
   → 「情報パネル」で各条件を確認
   
3. EMA が形成されていない
   → チャートに十分な過去データがあるか確認
   
4. 1時間足のトレンドが定まっていない
   → 200 EMA 付近の横ばい相場で動作しない仕様
```

### ❌ 問題：「アラートが鳴らない」

**症状：**
```
Use_Alerts = true でも、シグナル時にアラートが出ない
```

**原因と解決策：**
```
1. MT5のアラート設定が無効
   → 「ツール」→「オプション」→「通知」→「有効」確認
   
2. スピーカーがミュートになっている
   → Windows のボリュームを確認
   
3. サウンドファイルが設定されていない
   → オプション内の音声設定を確認
   
4. MetaTrader がバックグラウンドにある
   → MT5 をアクティブウィンドウにする
```

---

## 📝 詳細設定

### 🔊 アラーム音を変更

```
1. 「ツール」→「オプション」→「イベント」タブ
2. 「インジケーターアラート」の「ファイル」を選択
3. 任意の .wav ファイルを指定
```

### 📧 メール通知を設定

```
1. 「ツール」→「オプション」→「イベント」タブ
2. 「メール」セクションで設定
3. メールアドレスを入力
4. SMTP設定を確認
```

### 📱 プッシュ通知を設定

```
1. 「ツール」→「オプション」→「通知」タブ
2. MetaQuotes ID でログイン
3. デバイスを登録
```

---

## 🎓 使用例

### シナリオ1：毎日の取引前確認

```
朝 8:00（東京時間）
↓
1. MetaTrader 5 を起動
2. USDJPY の 5分足チャートを開く
3. インジケーターが自動で計算される
4. 情報パネルで環境確認
5. シグナルを待つ
```

### シナリオ2：複数通貨ペア監視

```
1. 4 つのチャートを同時に表示
   - USDJPY 5分足
   - EURJPY 5分足
   - EURUSD 5分足
   - XAUUSD 5分足

2. 各チャートにインジケーターを追加
3. タイル状に配置（「ウィンドウ」→「タイルを並べ替え」）
4. 複数シグナルを同時監視
```

---

## 📞 よくある質問（FAQ）

### Q1：インジケーターをコピーして別の通貨ペアに適用できる？
**A：** はい。同じインジケーターを複数チャートに適用できます。
```
各チャートで「挿入」→「インジケーター」→「カスタム」で追加するだけです。
```

### Q2：パラメータを後から変更できる？
**A：** はい。いつでも変更できます。
```
チャート右クリック → インジケーター → パラメータ変更
変更後は自動的に反映されます。
```

### Q3：バックテストで使える？
**A：** このインジケーターはリアルタイム描画用です。バックテストは別途 EA（Expert Advisor）が必要です。

### Q4：1分足や15分足でも使える？
**A：** 推奨しません。このインジケーターは「1時間足観測 + 5分足エントリー」専用に設計されています。

### Q5：複数の MT5 ターミナルがある場合？
**A：** 各ターミナルの MQL5\Indicators フォルダにコピーする必要があります。

---

## ✅ 実装完了チェックリスト

```
□ MetaTrader 5 がインストール済み
□ FX_Trading_Signals_1H5M.mq5 ファイルを入手
□ MQL5\Indicators フォルダを確認
□ ファイルを Indicators フォルダにコピー
□ MetaEditor でコンパイル（.ex5 生成を確認）
□ MT5 を再起動
□ 5分足チャートを開く
□ インジケーター追加（挿入 → カスタム → FX_Trading...）
□ パラメータ設定完了
□ チャート再描画（Ctrl+R）
□ EMA ラインが表示される
□ 情報パネルが表示される
□ シグナルが表示される（条件が揃えば）
□ アラーム設定確認

すべてにチェック → 実装完了 🎉
```

---

**バージョン：1.0 | 作成日：2026-08-12**
